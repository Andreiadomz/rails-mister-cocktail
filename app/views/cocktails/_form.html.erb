<%= simple_form_for(@cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.submit "add a mocktail", class: "btn btn-primary" %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
<% end %>

